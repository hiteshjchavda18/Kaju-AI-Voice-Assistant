{
  "outputDirectory": "frontend/dist",
  "buildCommand": "npm --prefix frontend run build",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}